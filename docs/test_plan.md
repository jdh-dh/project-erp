# 시험 계획서 (test_plan.md)

## 1. 개요

Phase 1 기능의 단위시험·통합시험 계획을 정의한다.

- 작성일: 2026-07-07
- 상태: 초안 (사용자 승인 대기)

## 2. 시험 환경

- Backend: pytest + httpx (FastAPI TestClient), 테스트 전용 PostgreSQL (docker-compose 또는 SQLite 대체 불가 — JSONB 사용으로 PostgreSQL 고정)
- Frontend: Vitest + React Testing Library
- CI: GitHub Actions에서 push 시 자동 실행 (Phase 1 내 구성)

## 3. 단위시험 (Unit Test)

서비스 계층 함수 단위로 시험한다.

| ID | 대상 | 시험 항목 |
|----|------|-----------|
| UT-AUTH-01 | 비밀번호 해시 | 해시 생성·검증, 평문 불일치 시 실패 |
| UT-AUTH-02 | JWT | 토큰 생성·검증, 만료 토큰 거부, 위조 토큰 거부 |
| UT-USER-01 | 사용자 서비스 | 등록, 이메일 중복 시 오류, 비활성화 |
| UT-CUST-01 | 고객사 서비스 | 등록, 수정, 담당자 추가, 프로젝트 연결 시 비활성화만 허용 |
| UT-PROJ-01 | 프로젝트 서비스 | 등록, 코드 중복 시 오류 |
| UT-PROJ-02 | 상태 전이 | 허용 전이 성공, 비허용 전이(예: COMPLETED→IN_PROGRESS) 거부 |
| UT-COM-01 | 변경 이력 | 생성·수정·상태변경 시 change_logs 기록 확인 |

## 4. 통합시험 (Integration Test)

API 엔드포인트 단위로 실제 DB를 사용해 시험한다.

| ID | 시나리오 |
|----|----------|
| IT-01 | 로그인 → 토큰 발급 → /auth/me 조회 → 토큰 재발급 |
| IT-02 | 잘못된 비밀번호 로그인 → 401, 비활성 사용자 로그인 → 401 |
| IT-03 | admin이 사용자 등록 → member 권한으로 사용자 등록 시도 → 403 |
| IT-04 | 고객사 등록 → 담당자 추가 → 상세 조회에 담당자 포함 확인 |
| IT-05 | 프로젝트 등록(고객사·PM 연결) → 상세 조회 → 참여자 설정 → 계약 등록 |
| IT-06 | 프로젝트 상태 변경 흐름 (PLANNED→IN_PROGRESS→COMPLETED) 및 이력 조회 |
| IT-07 | 중복 프로젝트 코드 등록 → 409 |
| IT-08 | 인증 없이 보호 API 호출 → 401 |

## 4.5 Phase 2 시험 — 일정 관리

### 단위시험

| ID | 대상 | 시험 항목 |
|----|------|-----------|
| UT-WBS-01 | WBS 서비스 | 등록, 계층(parent) 지정, 다른 프로젝트의 parent 거부 |
| UT-WBS-02 | WBS 서비스 | 자기 자신/순환 참조 parent 거부 |
| UT-WBS-03 | WBS 서비스 | 진척률 100 → 상태 DONE 자동 처리, 0~100 범위 검증 |
| UT-WBS-04 | 지연 판정 | end_date 경과 & 미완료 → is_delayed=true, DONE이면 false |
| UT-WBS-05 | 논리 삭제 | 비활성화 시 하위 항목 함께 비활성화 |
| UT-MS-01 | 마일스톤 서비스 | 등록, 달성 처리(달성일 기록), 지연 판정 |
| UT-SCH-01 | 일정 요약 | 진척률 평균·지연 수 계산 (비활성 항목 제외) |

### 통합시험

| ID | 시나리오 |
|----|----------|
| IT-09 | WBS 등록(계층 포함) → 목록 조회(계층·지연 포함) → 기간 변경 → change_logs에 기록 확인 |
| IT-10 | member 담당자가 자신의 항목 진척률 수정 성공, 타인 항목 수정 → 403 |
| IT-11 | member가 WBS 등록 시도 → 403 |
| IT-12 | 마일스톤 등록 → 달성 처리 → 일정 요약 조회 값 검증 |

## 4.6 Phase 3 시험 — 하드웨어/소프트웨어 관리

### 단위시험

| ID | 대상 | 시험 항목 |
|----|------|-----------|
| UT-HW-01 | 보드 서비스 | 등록, (이름, 리비전) 중복 409, 상태 변경 |
| UT-HW-02 | BOM 서비스 | 부품 추가·수정, 수량 1 미만 거부 |
| UT-HW-03 | 보드 비활성화 | BOM 연쇄 비활성화, 변경 이력 기록 |
| UT-HW-04 | 제작 이력 | 추가·조회 |
| UT-SW-01 | 모듈 서비스 | 등록, 이름 중복 409, 유형 검증 |
| UT-SW-02 | 버전 서비스 | 등록, 모듈 내 버전 중복 409 |
| UT-SW-03 | 릴리즈 처리 | 릴리즈일 기록, 중복 릴리즈 400 |
| UT-SW-04 | 빌드/배포 | 빌드(커밋 해시)·배포 이력 추가 및 상세 조회 포함 확인 |

### 통합시험

| ID | 시나리오 |
|----|----------|
| IT-13 | 보드 등록 → BOM 2건 추가 → 상세 조회(BOM 포함) → 제작 이력 추가 → 변경 이력 확인 |
| IT-14 | 모듈 등록 → 버전 등록 → 빌드 추가 → 배포 추가 → 릴리즈 처리 → 버전 상세 확인 |
| IT-15 | member가 보드/모듈 등록 시도 → 403 |

## 4.7 Phase 4 시험 — 시험/이슈/문서 관리

### 단위시험

| ID | 대상 | 시험 항목 |
|----|------|-----------|
| UT-TC-01 | 시험 케이스 | 등록, 유형(UNIT/INTEGRATION/FIELD) 검증, 수정·논리 삭제 |
| UT-TC-02 | 시험 실행 | PASS/FAIL 기록, 시험자 자동 기록, 최근 결과(last_result) 반영 |
| UT-ISS-01 | 이슈 | member 등록 가능(등록자 자동), 유형·심각도 검증 |
| UT-ISS-02 | 이슈 상태 | 전이 규칙 검증, RESOLVED 시 resolution 필수·해결일 기록, CLOSED 후 전이 불가 |
| UT-ISS-03 | 이슈 권한 | 담당자 수정 가능, 무관한 member 수정 403 |
| UT-DOC-01 | 문서 | 등록(작성자 자동), 유형 검증, 버전 수정 시 이력 기록 |
| UT-DOC-02 | 문서 권한 | member 등록 403 |

### 통합시험

| ID | 시나리오 |
|----|----------|
| IT-16 | 케이스 등록 → 실행 FAIL 기록 → 실행 PASS 기록 → 상세의 최근 결과 PASS 확인 |
| IT-17 | 이슈 등록(OPEN) → 담당자 지정 → IN_PROGRESS → 원인 분석 기록 → RESOLVED(조치 결과) → CLOSED → 변경 이력 확인 |
| IT-18 | 문서 등록 → 버전 갱신 → 목록·이력 확인 |

## 4.8 Phase 5 시험 — 릴리즈/보고서/비용

### 단위시험

| ID | 대상 | 시험 항목 |
|----|------|-----------|
| UT-REL-01 | 릴리즈 | 등록(등록자 자동), 버전 중복 409, 수정·논리 삭제·이력 |
| UT-COST-01 | 비용 | 등록(등록자 자동), 분류 검증, 금액 0 이하 거부 |
| UT-COST-02 | 비용 집계 | 총액·분류별 합계, 비활성 항목 제외 |
| UT-RPT-01 | 보고서 | 이슈 상태별·시험 최근결과별·SW 릴리즈 버전 수 집계 정확성 |

### 통합시험

| ID | 시나리오 |
|----|----------|
| IT-19 | 일정/이슈/시험/비용/릴리즈 데이터 등록 → 보고서 조회 → 모든 집계값 검증 |
| IT-20 | member가 릴리즈/비용 등록 시도 → 403 |

## 5. 합격 기준

- 계획된 단위시험·통합시험 전체 통과
- 서비스 계층 코드 커버리지 80% 이상
- 시험 결과는 verification.md 에 기록한다.
